# Color_meter 프로젝트 로드맵

## 프로젝트 목적 재정의

### 핵심 워크플로우
```
[타사 렌즈] → 측정/수치화 → [측정 데이터]
                              ↓
                         AI 분석 (계획)
                              ↓
                    자사 BOM에서 유사 잉크 추천
                    또는 레시피 조정 권고
                              ↓
                    [자사 렌즈 제작]
                              ↓
[자사 렌즈] → 측정/수치화 → [측정 데이터]
                              ↓
              타사 vs 자사 비교 분석
                              ↓
                    차이량/조정 필요량 도출
                              ↓
                    AI 조정 권고 (계획)
                              ↓
                         반복...
```

### 핵심 목표
타사 렌즈와 가장 유사한 자사 렌즈를 제작하기 위한 **반복적 측정-비교-조정 시스템**

---

## 현재 상태 vs 목적 Gap 분석

### 현재 구현된 것 (v7 엔진)

| 기능 | 상태 | 설명 |
|------|------|------|
| 렌즈 이미지 측정 | ✅ 완료 | 색상 추출, 서명 분석, 극좌표 변환 |
| 잉크 세그먼테이션 | ✅ 완료 | k-means 기반 자동 잉크 분리 |
| STD 등록 | ✅ 완료 | 표준 모델 학습 및 저장 |
| STD vs 테스트 비교 | ✅ 완료 | Delta E, 상관계수 계산 |
| 판정 시스템 | ✅ 완료 | OK/RETAKE/NG_COLOR/NG_PATTERN |
| SKU 설정 관리 | ✅ 완료 | Zone별 L*a*b*, 임계값 |

### 목적 달성을 위해 필요하지만 없는 것

| 기능 | Gap | 중요도 |
|------|-----|--------|
| 타사 렌즈 기준 측정 워크플로우 | 현재는 STD=자사 기준. 타사를 "Target"으로 삼는 흐름 없음 | 🔴 높음 |
| AI용 측정 데이터 Export | 구조화된 JSON/CSV 출력 형식 없음 | 🔴 높음 |
| BOM/레시피 데이터 구조 | 잉크 이름만 있음, 실제 배합비/원료 데이터 없음 | 🟡 중간 |
| 타사 vs 자사 비교 전용 기능 | 있지만 "타사=Target" 관점의 UI/API 없음 | 🔴 높음 |
| 차이량 → 조정량 변환 | Delta E는 있으나 "얼마나 조정" 계산 없음 (AI 영역) | 🟢 AI |
| 반복 조정 히스토리 | 이력 추적 시스템 없음 | 🟡 중간 |
| 잉크별 L*a*b* 상세 분석 | 개별 잉크의 L/a/b 채널별 편차 분석 | 🔴 높음 |

---

## 워크플로우 재설계

### 현재 워크플로우 (문제점)
```
[STD 이미지] → 등록 → [STD 모델]
[테스트 이미지] → 검사 → STD와 비교 → OK/NG 판정
```
- **문제**: STD = 자사 표준. "타사 렌즈를 따라가기" 관점 아님

### 제안 워크플로우 (목적에 맞춤)
```
Phase 1: 타사 렌즈 분석 (Target 설정)
─────────────────────────────────────
[타사 렌즈 이미지] → 분석 → [Target Profile]
                              ├─ 잉크별 L*a*b* 값
                              ├─ 잉크 분포/면적 비율
                              ├─ 색상 시그니처
                              └─ 패턴 특성

Phase 2: AI 분석 (계획만 - 미구현)
─────────────────────────────────────
[Target Profile] + [자사 BOM DB] → AI → [추천 잉크/레시피]

Phase 3: 자사 렌즈 제작 (외부 공정)
─────────────────────────────────────
추천대로 렌즈 제작

Phase 4: 비교 분석
─────────────────────────────────────
[자사 렌즈 이미지] → 분석 → [Sample Profile]
[Target Profile] vs [Sample Profile] → [차이 리포트]
                                        ├─ 잉크별 Delta E
                                        ├─ L/a/b 채널별 편차
                                        ├─ 시각화 (히트맵)
                                        └─ 요약

Phase 5: AI 조정 권고 (계획만 - 미구현)
─────────────────────────────────────
[차이 리포트] → AI → [조정 권고]
                      ├─ 어떤 잉크를 조정?
                      ├─ 어떤 방향으로? (더 밝게/어둡게, 채도 등)
                      └─ 얼마나?

Phase 6: 반복
─────────────────────────────────────
Phase 3~5 반복하여 차이 최소화
```

---

## AI 외 구현 필요 기능 목록

### 1단계: 핵심 기능 (필수)

#### 1.1 Target Profile 생성 API
**목적**: 타사 렌즈를 분석하여 "목표" 프로파일 생성

```
POST /api/v7/target/analyze
Request:
  - image: 타사 렌즈 이미지
  - sku_hint: SKU 힌트 (선택)
  - ink_count: 예상 잉크 개수 (선택)

Response:
  - target_id: 고유 ID
  - profile:
      - inks: [
          { name: "INK1", lab: {L, a, b}, area_ratio: 0.35, ... },
          { name: "INK2", lab: {L, a, b}, area_ratio: 0.40, ... },
          { name: "INK3", lab: {L, a, b}, area_ratio: 0.25, ... }
        ]
      - signature: { polar_data, ... }
      - pattern: { uniformity, ... }
  - visualization: { image_url, heatmap_url }
```

#### 1.2 잉크별 상세 분석 기능
**목적**: 각 잉크의 L/a/b 채널별 통계 제공

현재 구현:
```python
# color_masks.py - 클러스터 통계
cluster_stats = {
    "mean": [L, a, b],  # 평균
    "std": [L_std, a_std, b_std],  # 표준편차
}
```

추가 필요:
```python
ink_detail = {
    "ink_id": "INK1",
    "lab": {
        "L": {"mean": 45.2, "std": 2.1, "min": 40.5, "max": 50.3},
        "a": {"mean": 15.8, "std": 1.5, "min": 12.0, "max": 19.0},
        "b": {"mean": -42.3, "std": 3.2, "min": -48.0, "max": -36.0}
    },
    "area_ratio": 0.35,
    "pixel_count": 12500,
    "spatial_distribution": "radial",  # radial, uniform, clustered
}
```

#### 1.3 Target vs Sample 비교 API
**목적**: 타사(Target) vs 자사(Sample) 렌즈 직접 비교

```
POST /api/v7/compare/target-sample
Request:
  - target_id: Target 프로파일 ID (또는 target_image)
  - sample_image: 자사 렌즈 이미지

Response:
  - overall:
      - similarity_score: 0.85 (0~1)
      - overall_delta_e: 4.2
      - verdict: "CLOSE" | "NEEDS_ADJUSTMENT" | "FAR"
  - by_ink: [
      {
        ink: "INK1",
        delta_e: 3.1,
        delta_L: -1.2,  # 음수 = 자사가 더 어둡다
        delta_a: +0.8,
        delta_b: -2.1,
        area_diff: -0.02,  # 음수 = 자사가 면적 적음
        assessment: "자사 INK1이 약간 어둡고 파란기 부족"
      },
      ...
    ]
  - visualization: { side_by_side_url, diff_heatmap_url }
```

#### 1.4 AI용 데이터 Export 형식
**목적**: AI 모듈에 전달할 구조화된 데이터

```json
{
  "export_version": "1.0",
  "timestamp": "2026-01-14T...",
  "target": {
    "source": "competitor_lens_001.png",
    "inks": [...],
    "signature": {...}
  },
  "sample": {
    "source": "our_lens_v1.png",
    "inks": [...],
    "signature": {...}
  },
  "comparison": {
    "overall_delta_e": 4.2,
    "by_ink": [...],
    "recommendation_hint": {
      "INK1": {"direction": "increase_L", "magnitude": "small"},
      "INK2": {"direction": "decrease_b", "magnitude": "medium"}
    }
  },
  "context": {
    "iteration": 3,
    "previous_adjustments": [...],
    "bom_constraints": {...}
  }
}
```

### 2단계: 지원 기능

#### 2.1 히스토리 추적 시스템
```
/api/v7/project/{project_id}/iterations
  - iteration 1: target 설정
  - iteration 2: 첫 샘플 비교 (delta_e: 8.5)
  - iteration 3: 조정 후 비교 (delta_e: 5.2)
  - iteration 4: 재조정 후 비교 (delta_e: 3.1)
  - ...
```

#### 2.2 BOM 데이터 구조 (스키마만 - 실제 데이터는 별도)
```json
{
  "bom_id": "BOM_INK_BLUE_V3",
  "ink_name": "INK_BLUE",
  "components": [
    {"material": "Pigment_Blue_15", "ratio": 0.12},
    {"material": "Resin_A", "ratio": 0.45},
    {"material": "Solvent_X", "ratio": 0.40},
    {"material": "Additive_Y", "ratio": 0.03}
  ],
  "target_lab": {"L": 45, "a": 15, "b": -42},
  "tolerance": {"L": 3, "a": 2, "b": 3}
}
```

#### 2.3 조정 권고 데이터 구조 (AI 출력 형식 정의)
```json
{
  "recommendations": [
    {
      "ink": "INK1",
      "current_lab": {"L": 43.5, "a": 14.2, "b": -40.1},
      "target_lab": {"L": 45.2, "a": 15.8, "b": -42.3},
      "adjustment": {
        "L": "+1.7 (밝기 증가)",
        "a": "+1.6 (빨강 증가)",
        "b": "-2.2 (파랑 증가)"
      },
      "suggested_bom_change": "AI가 제공 (계획)",
      "confidence": 0.85
    }
  ]
}
```

---

## 현재 코드베이스 수정 계획

### Phase A: 기존 엔진 활용 (단기)

#### A.1 `single_analyzer.py` 확장
- 현재: `analyze_single()` → 검사 결과
- 추가: `build_target_profile()` → Target 프로파일 생성

#### A.2 비교 로직 분리
- 현재: `signature_compare.py` → STD vs Test
- 추가: `target_compare.py` → Target vs Sample (잉크별 상세)

#### A.3 Export 함수 추가
- 위치: `core/pipeline/feature_export.py` 확장
- 기능: AI용 JSON export

### Phase B: API 레이어 추가 (중기)

#### B.1 새 라우터: `routers/target.py`
```python
# POST /api/v7/target/analyze - Target 프로파일 생성
# GET /api/v7/target/{target_id} - Target 조회
# POST /api/v7/target/compare - Target vs Sample 비교
# GET /api/v7/target/{target_id}/export - AI용 export
```

#### B.2 프로젝트/히스토리 라우터: `routers/project.py`
```python
# POST /api/v7/project - 새 프로젝트 생성
# GET /api/v7/project/{id}/iterations - 반복 이력
# POST /api/v7/project/{id}/iteration - 새 반복 추가
```

### Phase C: 데이터 레이어 (장기)

#### C.1 Target 프로파일 저장
- 위치: `models/targets/{target_id}/`
- 형식: `profile.json`, `signature.npz`, `thumbnail.png`

#### C.2 프로젝트 DB
- 형식: SQLite 또는 JSON 파일
- 내용: 프로젝트 메타데이터, 반복 이력, 비교 결과

---

## 우선순위 정리

### 즉시 (1주 내)
1. ✅ 현재 `analyze_single()` 결과에서 잉크별 L/a/b 상세 추출 확인
2. 🔧 Target 프로파일 생성 함수 구현 (`build_target_profile()`)
3. 🔧 Target vs Sample 비교 함수 구현 (`compare_target_sample()`)

### 단기 (2-4주)
4. 🔧 AI용 Export JSON 형식 정의 및 구현
5. 🔧 `/api/v7/target/*` API 엔드포인트 추가
6. 🔧 간단한 프로젝트/히스토리 저장

### 중기 (1-2개월)
7. 🔧 비교 결과 시각화 개선 (side-by-side, diff heatmap)
8. 🔧 BOM 데이터 스키마 정의 (실제 데이터는 별도)
9. 📋 AI 모듈 인터페이스 정의 (계획)

### 장기 (3개월+)
10. 📋 AI 모듈 연동 (계획)
11. 📋 자동 조정 권고 시스템 (계획)

---

## 파일 구조 변경 계획

```
src/engine_v7/core/
├── target/                    # [NEW] Target 관리
│   ├── __init__.py
│   ├── target_profile.py      # Target 프로파일 생성
│   ├── target_compare.py      # Target vs Sample 비교
│   └── target_storage.py      # Target 저장/로드
│
├── export/                    # [NEW] AI용 Export
│   ├── __init__.py
│   ├── ai_export.py           # AI용 JSON 생성
│   └── schemas.py             # Export 스키마 정의
│
├── project/                   # [NEW] 프로젝트 관리
│   ├── __init__.py
│   ├── project_manager.py     # 프로젝트 CRUD
│   └── iteration_tracker.py   # 반복 이력 추적
│
└── (기존 모듈들 유지)

src/web/routers/
├── v7.py                      # (기존)
├── target.py                  # [NEW] Target API
└── project.py                 # [NEW] 프로젝트 API

data/
├── targets/                   # [NEW] Target 프로파일 저장
│   └── {target_id}/
│       ├── profile.json
│       ├── signature.npz
│       └── thumbnail.png
│
└── projects/                  # [NEW] 프로젝트 데이터
    └── {project_id}/
        ├── meta.json
        └── iterations/
            ├── 001/
            ├── 002/
            └── ...
```

---

## 다음 단계

1. **이 문서 검토**: 방향성 확인
2. **Phase A.1 시작**: `build_target_profile()` 함수 구현
3. **테스트**: 타사 렌즈 이미지로 Target 프로파일 생성 테스트
4. **Phase A.2**: 비교 로직 구현
5. **반복**: 피드백 기반 개선

---

## 관련 문서
- `docs/STATUS_v7_v2.md` - 현재 엔진 상태
- `INTEGRATION_STATUS.md` - 통합 진행 상황
- `src/engine_v7/configs/default.json` - 기본 설정
