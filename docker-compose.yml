version: "3.9"
services:
  app:
    build: .
    image: colormeter:latest
    environment:
      - LOG_LEVEL=INFO
      - SKU=SKU001
    volumes:
      - ./data:/app/data
      - ./config:/app/config
      - ./results:/app/results
    working_dir: /app
    entrypoint: ["python", "-m", "src.main"]
